# Traffic Signal Control Policy Explainability Project

## Overview

This project provides comprehensive model explainability analysis for a Deep Q-Network (DDQN) trained to control traffic signals. It features two complementary explanation methods:

- **LIME Explainer** (`lime_explainer.py`) - Uses Local Interpretable Model-agnostic Explanations for local interpretability
- **SHAP Explainer** (`shap_explainer.py`) - Uses SHAP (SHapley Additive exPlanations) for feature importance and global interpretability

Both methods help understand which detector features influence the traffic signal switching decisions.

## Key Components

### Scripts

**LIME Explainer** (`Code/lime_explainer.py`)
- Trains a LIME TabularExplainer on background states
- Explains individual test samples' switch/stay decisions
- Generates visualizations for global feature importance, detector heatmaps, and decision dominance
- Produces comprehensive visualizations with feature weights colored by influence direction

**SHAP Explainer** (`Code/shap_explainer.py`)
- Uses KernelExplainer for global feature attribution
- Identifies which features most influence P(switch|state)
- Includes multiple visualization types for comprehensive analysis
- Implements 10+ bug fixes for stability and accuracy

### Output Directories

- `lime_outputs/` - LIME explanation visualizations and raw data (`.npz` format)
- `shap_outputs/` - SHAP explanation visualizations and raw data (`.npz` format)
- `shap_outputs/*.png` - Individual visualization files

### Data & Models

- `policy_checkpoints_phase2/` - Trained DDQN model checkpoints (in `.gitignore`)
- `SUMO_FILES/` - SUMO traffic simulation network and route files
- `tree_outputs/` - Surrogate tree interpretations

## Project Structure

```
Project_root/
├── Code/
│   ├── lime_explainer.py
│   └── shap_explainer.py
├── lime_outputs/          # Generated by lime_explainer.py
│   ├── lime_feature_importance.png
│   ├── lime_detector_heatmap.png
│   ├── lime_global_features.png
│   └── lime_waterfall_switch.png
├── shap_outputs/          # Generated by shap_explainer.py
│   ├── shap_summary_beeswarm.png
│   ├── shap_feature_importance.png
│   ├── shap_detector_heatmap.png
│   ├── shap_waterfall_switch.png
│   └── ... (more visualization files)
├── SUMO_FILES/           # Traffic simulation data
│   ├── sim.net.xml
│   ├── sim.sumocfg
│   └── traffic_files/   # Generated route files
├── tree_outputs/          # Additional interpretability results
└── .gitignore            # Excludes model checkpoints
```

## Running the Analysis

### Prerequisites
- SUMO (Simulation of Urban MObility)
- Python 3.7+
- TensorFlow
- scikit-learn
- matplotlib
- numpy
- tf-agents
- lime
- shap

### Phase 2 Execution

The project implements a Phase 2 with improved code quality:

1. **Model Loading**: Reconstructs the Q-network architecture and loads trained weights
2. **State Collection**: Runs SUMO rollouts to collect observation states
3. **Explainability**: Computes feature importance using LIME or SHAP
4. **Visualization**: Generates comprehensive interpretation plots

### Running LIME Explainer

```bash
python3 Code/lime_explainer.py
```

### Running SHAP Explainer

```bash
python3 Code/shap_explainer.py
```

## Key Features & Fixes

### SHAP Explainer Improvements

The SHAP version includes these critical fixes:

1. **SAFE PARSING**: Robust checkpoint variable extraction with `_safe_sort_key()`
2. **OPTIMIZER FILTERING**: Excludes optimizer/Adam weights from loading
3. **DYNAMIC PHASES**: Live SUMO query for accurate phase count
4. **GLOBAL STATE**: `matplotlib.rcdefaults()` at module level
5. **EXPLAINER REUSE**: Single explainer instance for consistent results
6. **AUTO INTERACTION**: `interaction_index='auto'` for dependency plots
7. **DIRECT CALLS**: Eliminates `generate_and_collect()` wrapper
8. **PATH VERIFICATION**: Checks `variables/variables.index` instead of `saved_model.pb`
9. **SOFTMAX DIAGNOSIS**: Logs mean probabilities to detect saturation
10. **TRAFFIC SAFETY**: Handles vehicle cleanup during rollouts

### LIME Explainer Features

- Stratified test sets for balanced switch/stay coverage
- Comprehensive visualization suite
- Single run generates multiple insights

## Analysis Outputs

### Global Feature Importance

Both explainers produce bar charts showing mean absolute feature weights, ranked by importance.

### Detector Influence

6 detectors × 5 features grid showing which detectors and features most influence switching decisions.

### Non-Detector Features

Analysis of temporal and contextual features (time_since_switch, phase one-hot, pressure, demand).

### Sample-specific Explanations

- **Waterfall plots**: Show feature contributions for individual samples
- **Dependence plots**: Visualize feature interaction effects
- **Decision dominance**: Identify which detector drives each decision

### Model Health Checks

- Action rate validation (catches collapsed policies)
- Softmax saturation detection
- State dimension verification

## Use Cases

1. **Traffic Engineer**: Understand why the signal controller makes switching decisions
2. **ML Researcher**: Compare LIME vs SHAP for temporal sequential decision problems
3. **Simulator Developer**: Validate SUMO-trained policies with explainability tools
4. **Policy Analyst**: Document decision logic for regulatory compliance

## Technical Details

### State Vector (37 dimensions)

6 detectors × 5 features:
- Queue (vehicles)
- Occupancy (%)
- Speed (km/h)
- Mean waiting time (s)
- Emergency vehicle count

+
1. Time since last switch (normalized)
2. Phase one-hot encoding (dynamic length)
3. Pressure (normalized total queue)
4. Demand (normalized vehicle count)

### Decision Logic

The DDQN policy outputs Q-values for two actions:
- Action 0: Stay in current phase
- Action 1: Switch to next phase

Green time minimum: 25 simulation steps to prevent frequent switching.

## Dependencies

```python
# Core dependencies
c Rasa
tf-agents

# Explanation libraries
lime-shap
matplotlib

# Analysis
numpy
pandas

# Simulation
sumo (external)
```

## Visualization Gallery

All analysis results are saved as PNG files in the respective output directories. The visualizations provide:

- **Global insights** into feature importance patterns
- **Local explanations** for specific driving scenarios
- **Diagnostic plots** for model health monitoring
- **Comparison charts** between detectors and features

## Future Enhancements

Potential extensions for this framework:

1. **Temporal Analysis**: Add LSTM-based explanations for sequence patterns
2. **Multi-objective**: Extend to green time optimization alongside switching
3. **Real-time Monitoring**: Deploy explanations in operational traffic centers
4. **Comparative Studies**: Run both LIME and SHAP for method comparison

## Citation

If you use this project or its methodologies, please cite:

```bibtex
@article{traffic_signal_explainability_2026,
  title={Model Explainability for Deep Reinforcement Learning in Traffic Signal Control},
  author={Implementation Team},
  year={2026},
  journal={Traffic Engineering and Intelligent Transportation Systems}
}
```

## Support & Contact

For questions, issues, or collaboration opportunities:

- GitHub Issues
- Documentation requests
- Bug reports
- Feature suggestions

The project is actively maintained and accepts contributions following standard open-source practices.
